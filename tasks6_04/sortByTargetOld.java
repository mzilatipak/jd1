package by.htp.tasks6_04;

public class sortByTargetOld {
/*	Train[] targets = new Train[5];
	private int index = 0;

	public void addTarget(Train train) {
		targets[index] = train;
    	index++;
		}

	public Train[] getTarget() {
		return targets;
		}
    
    public Train[] sortByTarget(Train[] targets) {
    	for (int i = 0; i < targets.length; i++) {
    		for (int j = 0; j < targets.length - 1; j++) {
    			rez = sortByTarget(targets[j], targets[j+1]);
    			if ( rez == -1) {
    			}else if ( rez == 1) {
    			}
    		}
    	}
    	
    	public static sortByTarget(Train train, Train target) {
    		int min;
    		if (targets.length() < targets.length()) {
    			min = targets.length();
    			}else {
    			min = targets.length();
    			}
    			
    			int i = 0;
    			while ( i < min) {
    			if (targets.charAt(i) < targets.charAt(i)) {
    			return -1;
    			}else if (targets.charAt(i) > targets.charAt(i)) {
    			return 1;
    			}else {
    			}
    			i++;
    		}
    		if (targets.charAt(i) < targets.charAt(i)) {
    			return -1;
    			}else if (targets.charAt(i) > targets.charAt(i)) {
    			return 1;
    			}else {
    			return 0;
    			}
    		}
 /*   	for (int i = 0; i < targets.length; i++) {
    		for (int j = 0; j < targets.length - 1 - i; j++) {
        		if (targets[j].getTime() > targets[j + 1]) {
        			tmp = 
        		}
        	}
    	}	*/
    }
